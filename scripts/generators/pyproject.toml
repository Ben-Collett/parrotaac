[project]
name = 'generators'
version = '0.1.0'
requires-python = '>=3.11'
dependencies = [
    'tree-sitter>=0.20.0',
    'tree-sitter-language-pack>=1.0.0',
    'pytest>=8.0.0',
]

[tool.pytest.ini_options]
testpaths = ['.']